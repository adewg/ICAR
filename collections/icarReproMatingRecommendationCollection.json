{
  "description": "Represents a collection of animal mating recommendation events. Based on icarResourceCollection to provide paging etc.",

  "type": "object",

  "allOf": [{
      "$ref": "../collections/icarResourceCollection.json"
    },
    {
      "properties": {
        "member": {
          "type": "array",
          "items": {
            "$ref": "../resources/icarReproMatingRecommendationResource.json"
          },
          "description": "As per JSON-LD Hydra syntax, member provides the array of objects, in this case mating recommendation events."
        }
      }
    }
  ]
}