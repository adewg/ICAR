{
  "description": "Represents a collection of weaning events for groups of animals.",

  "allOf": [{
      "$ref": "../collections/icarResourceCollection.json"
    },
    {
      "type": "object",

      "properties": {
        "member": {
          "type": "array",
          "items": {
            "$ref": "../resources/icarGroupWeaningEventResource.json"
          },
          "description": "As per JSON-LD Hydra syntax, member provides the array of objects, in this case group weaning events."
        }
      }
    }
  ]
}
