      "icarHealthReportType": {
        "required": [
          "timestamp"
        ],
        "type": "object",
        "properties": {
          "identifier": {
            "$ref": "#/components/schemas/animalIdentifier"
          },
          "timestamp": {
            "type": "string",
            "description": "The timestamp of the measurement.",
            "format": "date-time"
          },
          "vendorSpecific": {
            "$ref": "#/components/schemas/icarHealthReportVendorSpecific"
          }
        },
        "description": "Gives health information."
      },
