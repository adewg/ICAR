      "icarHeatActivityType": {
        "required": [
          "identifier"
        ],
        "type": "object",
        "properties": {
          "identifier": {
            "$ref": "#/components/schemas/animalIdentifier"
          },
          "timestamp": {
            "type": "string",
            "description": "The timestamp of the measurement.",
            "format": "date-time"
          },
          "vendorSpecific": {
            "$ref": "#/components/schemas/icarHeatActivityVendorSpecific"
          }
        },
        "description": "Gives information about an in-heat activity of a dam."
      },
